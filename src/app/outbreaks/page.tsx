import OutbreakTracker from "@/components/OutbreakTracker";

export const metadata = { title: "Outbreak Tracker — The Theorist" };

export default function OutbreaksPage() {
  return <OutbreakTracker />;
}
